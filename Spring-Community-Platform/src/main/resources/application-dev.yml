server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springsecurity?serverTimezone=Asia/Seoul
    username: root
    password: '0000'

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true

  # 로컬에서는 기존처럼 Gmail을 사용
  mail:
    host: smtp.gmail.com
    port: 587
    username: kmh000904@gmail.com
    password: sloejykunbgsmrdr
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true